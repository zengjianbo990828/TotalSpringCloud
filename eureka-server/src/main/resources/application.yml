server:
  port: 10086 # 服务端口
spring:
  application:
    name: eurekaserver # eureka的服务名称
eureka:
  client:
    service-url:  # eureka的地址信息，如果有多个则用  , 隔开
      defaultZone: http://127.0.0.1:10086/eureka